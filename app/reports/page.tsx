"use client";import {useState} from "react";export default function Reports(){const [open,setOpen]=useState(false),[mode,setMode]=useState("client"),[format,setFormat]=useState("csv"),[from,setFrom]=useState(""),[to,setTo]=useState(""),[status,setStatus]=useState("");const url=`/api/export?mode=${mode}&format=${format}${from?`&from=${from}`:""}${to?`&to=${to}`:""}${status?`&status=${status}`:""}`;return <><h2>Отчёты</h2><section className="calculator"><label>С даты<input type="date" value={from} onChange={e=>setFrom(e.target.value)}/></label><label>По дату<input type="date" value={to} onChange={e=>setTo(e.target.value)}/></label><label>Статус<select value={status} onChange={e=>setStatus(e.target.value)}><option value="">Все</option><option value="DRAFT">Черновик</option><option value="PROCESSING">В работе</option><option value="COMPLETED">Завершена</option><option value="CANCELLED">Отменена</option></select></label></section><button onClick={()=>setOpen(true)}>Скачать отчёт</button>{open&&<div className="modal-backdrop" onClick={()=>setOpen(false)}><section className="modal" onClick={e=>e.stopPropagation()}><h3>Настройка отчёта</h3><label>Состав отчёта<select value={mode} onChange={e=>setMode(e.target.value)}><option value="client">Отчёт для клиента</option><option value="base">Внутренний: без дополнительного дохода</option><option value="full">С дополнительным доходом</option></select></label><label>Формат<select value={format} onChange={e=>setFormat(e.target.value)}><option value="csv">CSV</option><option value="xlsx">XLSX</option><option value="pdf">PDF-сводка</option></select></label><a className="download" href={url} onClick={()=>setOpen(false)}>Скачать отчёт</a><button className="cancel" onClick={()=>setOpen(false)}>Отмена</button></section></div>}</>}


