import "./globals.css";import {getCurrentActor} from "@/src/lib/auth";import Navigation from "@/src/components/navigation";
export const metadata={title:"Финансовый учёт",description:"Учёт операций"};
export default async function RootLayout({children}:{children:React.ReactNode}){const user=await getCurrentActor();return <html lang="ru"><body>{user&&<Navigation user={user}/>}<main className={user?"":"login-main"}>{children}</main></body></html>}