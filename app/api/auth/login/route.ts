import {NextResponse} from "next/server";import bcrypt from "bcryptjs";import {z} from "zod";import {prisma} from "@/src/lib/db";import {createSession,sessionCookie} from "@/src/lib/auth";
const body=z.object({login:z.string().min(3).max(120),password:z.string().min(6).max(200)});
export async function POST(req:Request){const p=body.safeParse(await req.json());if(!p.success)return NextResponse.json({error:"Введите логин и пароль"},{status:400});let user=await prisma.user.findUnique({where:{email:p.data.login}});const ownerLogin=process.env.LOCAL_ADMIN_EMAIL;if(!user&&ownerLogin&&p.data.login===ownerLogin&&process.env.LOCAL_ADMIN_PASSWORD===p.data.password){user=await prisma.user.create({data:{email:ownerLogin,name:"Владелец",passwordHash:await bcrypt.hash(p.data.password,10),role:"OWNER"}})}if(!user||!(await bcrypt.compare(p.data.password,user.passwordHash)))return NextResponse.json({error:"Неверный логин или пароль"},{status:401});const response=NextResponse.json({user:{name:user.name,role:user.role}});response.cookies.set(sessionCookie,createSession(user.id),{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/",maxAge:60*60*24*14});return response}