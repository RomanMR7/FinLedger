"use client";import {FormEvent,useEffect,useState} from "react";import {fetchJson} from "@/src/lib/client";type User={id:string;name:string;email:string;role:string};export default function Users(){const [users,setUsers]=useState<User[]>([]),[name,setName]=useState(""),[login,setLogin]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState("");const load=async()=>{const r=await fetchJson<User[]>("/api/users",[]);setUsers(r.data)};useEffect(()=>{load()},[]);async function add(e:FormEvent){e.preventDefault();const r=await fetch("/api/users",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name,login,password})});if(r.ok){setName("");setLogin("");setPassword("");setMessage("Трейдер добавлен");load()}else setMessage((await r.json()).error||"Не удалось добавить трейдера")}return <><h2>Пользователи</h2><p>Трейдер видит и изменяет только свои операции.</p><form onSubmit={add} className="grid"><label>Имя<input value={name} onChange={e=>setName(e.target.value)} required/></label><label>Логин<input value={login} onChange={e=>setLogin(e.target.value)} required/></label><label>Пароль<input type="password" value={password} onChange={e=>setPassword(e.target.value)} minLength={8} required/></label><button>Добавить трейдера</button></form><p>{message}</p><table><thead><tr><th>Имя</th><th>Логин</th><th>Роль</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.name}</td><td>{u.email}</td><td>{u.role==="OWNER"?"Владелец":"Трейдер"}</td></tr>)}</tbody></table></>}