import {Prisma} from "@prisma/client";
const d=(v:unknown)=>new Prisma.Decimal(v as Prisma.Decimal.Value);const rub=(v:Prisma.Decimal)=>v.toDecimalPlaces(2,Prisma.Decimal.ROUND_HALF_UP);const usdt=(v:Prisma.Decimal)=>v.toDecimalPlaces(6,Prisma.Decimal.ROUND_HALF_UP);const rate=(v:Prisma.Decimal)=>v.toDecimalPlaces(4,Prisma.Decimal.ROUND_HALF_UP);
export function calculateDealMoney(input:{amountRub:Prisma.Decimal.Value;tariffPercent:Prisma.Decimal.Value;clientRate:Prisma.Decimal.Value;actualRate:Prisma.Decimal.Value;expenses?:Prisma.Decimal.Value}){const amount=d(input.amountRub),percent=d(input.tariffPercent),client=rate(d(input.clientRate)),actual=rate(d(input.actualRate)),expenses=d(input.expenses??0);const tariff=rub(amount.mul(percent).div(100));const settlement=rub(amount.minus(tariff));const payout=usdt(settlement.div(client));const cost=rub(payout.mul(actual));const exchange=rub(settlement.minus(cost));const total=rub(tariff.plus(exchange).minus(expenses));return {tariffProfit:tariff,settlementRub:settlement,payoutUsdt:payout,actualCostRub:cost,exchangeProfitRub:exchange,totalProfitRub:total,clientRate:client,actualRate:actual};}
